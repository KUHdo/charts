apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcsql.fullname" . }}-test-connection"
  labels:
    {{- include "mcsql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mcsql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
